$(document).ready(function(){



	// let btns  = $('a#add_friends');
	
	// for(let i = 0; i < btns.length; i++ ){
	// 	let btn = btns[i];

	// 	$(btn).each(function(){
			
	// 		$(this).on('click', function(ev){

	// 			ev.preventDefault();

	// 			console.log(btn)
				

	// 			let query = new XMLHttpRequest();

	// 			query.onreadystatechange = function() {
	
	// 				if( query.readyState === 4 && query.status === 200){
						
	// 					setTimeout(() => {
	
	// 						$(btn).text("Follow");
	// 						$(btn).parent().css('background-color', '#1c94e0')
	
	// 					}, 2000);	
	// 				}
	// 			}
	// 			let data = {
	// 				id: 'id',
	// 				pseudo: 'pseudo'
	// 			}

	// 			query.open ("GET", $(this).attr('href'), true );
	// 			query.send ( data );
	// 		})

	// 	});
	// }


		

			

		






});
